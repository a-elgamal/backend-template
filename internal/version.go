// Package internal has all the packages internal to the service
package internal

var (
	// Version the build version
	Version string
	// GitTag the git tag associated with the build
	GitTag string
	// GitCommit the git commit associated with the build
	GitCommit string
	// BuildDate the build time
	BuildDate string
)
